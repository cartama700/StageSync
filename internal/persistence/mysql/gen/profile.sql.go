// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: profile.sql

package gen

import (
	"context"
	"time"
)

const createProfile = `-- name: CreateProfile :exec
INSERT INTO profiles (id, name, created_at)
VALUES (?, ?, ?)
`

type CreateProfileParams struct {
	ID        string
	Name      string
	CreatedAt time.Time
}

func (q *Queries) CreateProfile(ctx context.Context, arg CreateProfileParams) error {
	_, err := q.db.ExecContext(ctx, createProfile, arg.ID, arg.Name, arg.CreatedAt)
	return err
}

const getProfile = `-- name: GetProfile :one

SELECT id, name, created_at
FROM profiles
WHERE id = ?
`

// sqlc 쿼리 파일 — 각 쿼리는 '-- name: Xxx :type' 주석으로 이름·반환 형태 선언.
// :one  → 단일 row · 없으면 sql.ErrNoRows
// :many → 여러 row
// :exec → 실행만 (INSERT/UPDATE/DELETE)
// :execrows → 실행 + 영향받은 row 수 반환
func (q *Queries) GetProfile(ctx context.Context, id string) (Profile, error) {
	row := q.db.QueryRowContext(ctx, getProfile, id)
	var i Profile
	err := row.Scan(&i.ID, &i.Name, &i.CreatedAt)
	return i, err
}
